(library
  (name domain_name)
  (public_name domain-name)
  (modules domain_name)
  (libraries fmt astring)
  (ocamlopt_flags (-S)))

(test
  (name tests)
  (modules tests)
  (libraries alcotest domain-name))
